services:
  postgres:
    image: postgres:16-alpine
    container_name: crewcheck-postgres
    environment:
      POSTGRES_DB: crewcheck
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
